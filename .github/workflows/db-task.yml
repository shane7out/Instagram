name: db-task
on:
  push:
    branches: [claude/master-file-e6ofy0]
    paths: ['.github/db-task/task.sh']
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - run: bash .github/db-task/task.sh
